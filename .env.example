# SINESP-as-a-Service — Environment Variables
# Copy to .env and adjust as needed

# API
API_HOST=0.0.0.0
API_PORT=8000

# Rate limit (requests per minute per IP)
RATE_LIMIT_RPM=60

# Data refresh
SNAPSHOT_DIR=data/snapshots

# Logging level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
